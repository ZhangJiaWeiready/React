/** 左侧的信息输入添加区域* */import React,{Component} from 'react'import PropTypes from 'prop-types'export default class Add extends Component {    static propTypes = {        addComment:PropTypes.func.isRequired    }    submitVal = () => {        let commen = {}        const name = this.refs.imputName.value.trim()        const discuss = this.refs.imputMsg.value.trim()        const {addComment} = this.props        //进行判断  如果没有名字或者内容的话 不会继续下去        if(!name || !discuss){            return        }        commen = {            name,            discuss        }        addComment(commen)        this.refs.imputName.value=""        this.refs.imputMsg.value=""    }    render () {        return (            <div className="col-md-4">                <form className="form-horizontal">                    <div className="form-group">                        <label>用户名</label>                        <input type="text" className="form-control" placeholder="用户名"  ref="imputName"/>                    </div>                    <div className="form-group">                        <label>评论内容</label>                        <textarea className="form-control" rows="6" placeholder="评论内容" ref="imputMsg"></textarea>                    </div>                    <div className="form-group">                        <div className="col-sm-offset-2 col-sm-10">                            <button type="button" className="btn btn-default pull-right" onClick={this.submitVal}>提交</button>                        </div>                    </div>                </form>            </div>        )    }}