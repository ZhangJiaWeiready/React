/** 列表里面的详细内容* */import React,{Component} from 'react'import PropTypes from 'prop-types'import './comment-item.css'export default class item extends Component {    static propTypes = {        comment: PropTypes.object.isRequired,        index:PropTypes.number.isRequired,        removeDiscuss:PropTypes.func.isRequired    }    removeDiscuss = () => {        const {comment,index,removeDiscuss} = this.props        if(window.confirm(`确定删除${comment.name}的评论吗?`)){            removeDiscuss(index)        }    }    render () {        const {comment,index} = this.props        return (            <li className="list-group-item">                <div className="handle">                    <a href="javascript:;" onClick={this.removeDiscuss}>删除</a>                </div>                <p className="user"><span>{comment.name}</span><span>说:</span></p>                <p className="centence">{comment.discuss}</p>            </li>        )    }}