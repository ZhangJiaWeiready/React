/** 右侧的列表 结构* */import React,{Component} from 'react'import PropTypes from 'prop-types'import CommentItem from '../comment-item/comment-item'import './comment-list.css'export default class List extends Component {    static propTypes = {        comments:PropTypes.array.isRequired,        removeDiscuss:PropTypes.func.isRequired    }    render () {        const {comments,removeDiscuss} = this.props        const isShow = comments.length? "none":"display"        return (            <div className="col-md-8">                <h3 className="reply">评论回复：</h3>                <h2 style={{display: {isShow}}}>暂无评论，点击左侧添加评论！！！</h2>                <ul className="list-group">                    {                        comments.map((comment, index) => <CommentItem key={index} comment={comment} removeDiscuss={removeDiscuss} index={index}/>)                    }                </ul>            </div>        )    }}