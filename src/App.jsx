/** 主要的 大包裹器的结构* */import React,{Component} from 'react'import Add from './component/comment-add/comment-add'import List from './component/comment-list/comment-list'export default class App extends Component {    state = {        comments:[            {                name:"silly",                discuss:"你说的不对"            },            {                name:"boby",                discuss:"好厉害"            }        ]    }    addComment = (commentMsg) => {        //这里更新状态        const {comments} = this.state        comments.unshift(commentMsg)        this.setState({            comments        })    }    removeDiscuss = (discuss) =>{        const {comments} = this.state        comments.splice(discuss,1)        this.setState({            comments        })    }    render () {        const {comments} = this.state        return (            <div>                <header className="site-header jumbotron">                    <div className="container">                        <div className="row">                            <div className="col-xs-12">                                <h1>请发表对React的评论</h1>                            </div>                        </div>                    </div>                </header>                <div className="container" >                   <Add addComment={this.addComment}/>                   <List comments={comments} removeDiscuss={this.removeDiscuss}/>                </div>            </div>        )    }}